"""Pipeline run schemas.

Defines RunStatus / StageStatus enums and the run schema that tracks a
single pipeline execution (keyed by run_id) as it moves through the
parse -> categorize -> analyze -> recommend stages.
"""
