"""Normalized transaction schemas.

Represents a single normalized financial record extracted from a
document (date, amount, description, direction) plus the category
and recurrence/subscription flags assigned by the categorization agent.
"""
