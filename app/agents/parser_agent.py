"""Document Parser Agent (LLM-driven).

Parses uploaded files (PDF/CSV/Excel/images) and extracts structured,
normalized financial records (transactions, income, balances) for a run.
Reads the uploaded document(s) for a run_id, writes normalized records.
"""

